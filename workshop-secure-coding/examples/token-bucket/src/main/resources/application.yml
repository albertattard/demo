spring:
  application:
    name: 'Token Bucket'

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always

logging:
  level:
    org.springframework.web: DEBUG

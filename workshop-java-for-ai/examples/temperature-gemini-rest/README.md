[//]: # (Automatically generated by Sociable Weaver)
# Temperature - Gemini using REST API

This simple example demonstrates how temperature affects the model’s responses.
At lower temperatures, such as 0, the output is more deterministic, while higher
temperatures produce more varied responses.

To maintain simplicity, no external libraries are used. The purpose of this
example is not to illustrate how to interact with the model, but rather to
highlight the effect of temperature on its output. For actual interaction with a
model, it is advisable to use an API such as
[langchain4j](https://github.com/langchain4j/langchain4j) or
[Spring AI](https://spring.io/projects/spring-ai).

This example interacts with Google’s Gemini AI, hosted on Google Cloud. An API
key is required, available from
[Google AI Studio](https://aistudio.google.com/apikey). At the time of writing,
Google offers a generous free tier, which is more than sufficient for this
example.

---

## Disclaimer

The examples provided in this repository are for **educational purposes only**
and are intended to be used **exclusively within this workshop**. While every
effort has been made to ensure accuracy and reliability, these examples are
provided **“as is”**, without any warranties, express or implied.

By using these examples, you acknowledge that you do so **at your own risk**.
The authors and contributors shall **not be held liable** for any direct,
indirect, incidental, or consequential damages resulting from the use, misuse,
or inability to use these examples.

It is the responsibility of the user to **review, test, and validate** any code
before applying it in a production or commercial environment.

### Performance Disclaimer

The performance values shown in this workshop are for reference only and should
not be treated as absolute. Results can vary depending on hardware, system
configuration, runtime environment, and other factors. Readers are encouraged to
conduct their own tests under controlled conditions to obtain accurate
measurements relevant to their specific use case.

---

## Prerequisites

- [Oracle Java 25](https://www.oracle.com/java/technologies/downloads/#java25)
- [Google AI Studio API Key](https://aistudio.google.com/apikey)

## Run the example

1. Google AI Studio API Key

   If you have not already done so, please create an API key from
   [Google AI Studio](https://aistudio.google.com/apikey). This example searches
   for the key in the following order:

    1. System property: `gemini.api.key`
       (e.g. `java -Dgemini.api.key=[API-KEY] ...`)
    2. Environment variable: `GEMINI_API_KEY`
       (e.g. `GEMINI_API_KEY=[API-KEY] java ...`)
    3. File: `~/.gemini/api.key`

   For security purposes and to prevent exposing the API key in documentation,
   the following commands assume the key is saved in the `.gemini/api.key` file
   within the user’s home directory.

2. Build the application

   This step assumes that a valid Google AI Studio API key is available at
   `~/.gemini/api.key`. See Step 1 for more information about how to do this.

   ```shell
   ./mvnw clean verify
   ```

   The above command also runs tests that prompt the model twice with the same input
   and a temperature of 0. While the responses should be identical, this is not
   guaranteed, as the requests may be processed by different hardware, potentially
   resulting in varying outputs. Consequently, the test may fail.

   (_Optional_) Verify that the application was created.

   ```shell
   tree --sort=name './target/libs'
   ```

   The application JAR file.

   ```
   ./target/libs
   └── temperature-gemini-rest-1.0.0.jar

   0 directories, 1 file
   ```

3. Run the program

   This step assumes that a valid Google AI Studio API key is available at
   `~/.gemini/api.key`. See Step 1 for more information about how to do this.

   Run the example with the default temperature of `0`.

   ```shell
   java \
     --class-path './target/libs/*' \
     demo.Main
   ```

   The temperature is low, which means that the output from the model will be
   consistent between runs.

   ```
   prompt> When did humans first land on the Moon?
   temperature> 0.0
   assistant> Humans first landed on the Moon on **July 20, 1969**, during the **Apollo 11** mission.

   The astronauts Neil Armstrong and Buzz Aldrin were the first two people to walk on the lunar surface.
   ```

   Run the example again with the default temperature of `0`.

   ```shell
   java \
     --class-path './target/libs/*' \
     demo.Main
   ```

   As previously mentioned, a temperature of `0` does not guarantee identical
   output, even though it theoretically should. The requests may be handled by
   different hardware, potentially leading to variations in the model’s
   response.

   ```
   prompt> When did humans first land on the Moon?
   temperature> 0.0
   assistant> Humans first landed on the Moon on **July 20, 1969**, during the **Apollo 11** mission.

   The astronauts Neil Armstrong and Buzz Aldrin were the first two people to walk on the lunar surface.
   ```

   Run the example again with the higher temperature, such as `2`.

   ```shell
   java \
     --class-path './target/libs/*' \
     demo.Main 2
   ```

   The high temperature may cause the model to generate varying responses to the
   same prompt.

   ```
   prompt> When did humans first land on the Moon?
   temperature> 2.0
   assistant> Humans first landed on the Moon on **July 20, 1969**.

   This historic event was accomplished by the United States' Apollo 11 mission, with astronaut Neil Armstrong being the first person to step onto the lunar surface, followed shortly by Buzz Aldrin.
   ```

   Run the example again with the same high temperature.

   ```shell
   java \
     --class-path './target/libs/*' \
     demo.Main 2
   ```

   The high temperature may cause the model to generate varying responses to the
   same prompt.

   ```
   prompt> When did humans first land on the Moon?
   temperature> 2.0
   assistant> Humans first landed on the Moon on **July 20, 1969**.

   This historic achievement was accomplished by the United States' **Apollo 11 mission**. Astronauts Neil Armstrong and Buzz Aldrin were the first two people to step onto the lunar surface, with Michael Collins orbiting above in the command module.
   ```

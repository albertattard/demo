<!-- Generated by Sociable Weaver (https://github.com/albertattard/sw). Manual edits will be overwritten when regenerated. -->

# Prompt - ChatGPT API

This simple example demonstrates how to prompt
[ChatGPT](https://platform.openai.com/) using their client API. It performs a
single prompt and does not engage in a conversational exchange with the model.

The example directly utilises ChatGPT’s specific API, which may result in the
application being tightly coupled to this particular model unless an abstraction
layer is implemented. A more flexible approach would be to use an API such as
[langchain4j](https://github.com/langchain4j/langchain4j) or
[Spring AI](https://spring.io/projects/spring-ai).

This example interacts with the hosted OpenAI’s ChatGPT. An API key is required,
available from [OpenAI Platform](https://platform.openai.com/). Please note that
payment is required.

---

## Disclaimer

The examples provided in this repository are for **educational purposes only**
and are intended to be used **exclusively within this workshop**. While every
effort has been made to ensure accuracy and reliability, these examples are
provided **“as is”**, without any warranties, express or implied.

By using these examples, you acknowledge that you do so **at your own risk**.
The authors and contributors shall **not be held liable** for any direct,
indirect, incidental, or consequential damages resulting from the use, misuse,
or inability to use these examples.

It is the responsibility of the user to **review, test, and validate** any code
before applying it in a production or commercial environment.

### Performance Disclaimer

The performance values shown in this workshop are for reference only and should
not be treated as absolute. Results can vary depending on hardware, system
configuration, runtime environment, and other factors. Readers are encouraged to
conduct their own tests under controlled conditions to obtain accurate
measurements relevant to their specific use case.

---

## Prerequisites

- [Oracle Java 25](https://www.oracle.com/java/technologies/downloads/#java25)

- [OpenAI API Key](https://platform.openai.com/api-keys)

- [`jq`](https://jqlang.org/)

## Run the example

1. OpenAI API Key

   If you have not already done so, please create an API key from
   [OpenAI API Key](https://platform.openai.com/). This example searches
   for the key in the following order:

    1. System property: `chatgpt.api.key`
       (e.g. `java -Dchatgpt.api.key=[API-KEY] ...`)
    2. Environment variable: `CHATGPT_API_KEY`
       (e.g. `CHATGPT_API_KEY=[API-KEY] java ...`)
    3. File: `~/.chatgpt/api.key`

   For security purposes and to prevent exposing the API key in documentation,
   the following commands assume the key is saved in the `.chatgpt/api.key` file
   within the user’s home directory.

   Verify that the OpenAI APIs can be successfully invoked. The following
   command assumes that the API key is stored at `~/.chatgpt/api.key`.

   ```shell
   curl https://api.openai.com/v1/chat/completions \
     --silent \
     --header "Authorization: Bearer $(cat ~/.chatgpt/api.key)" \
     --header "Content-Type: application/json" \
     --data '{
     "model": "gpt-4o-mini",
     "messages": [
       {
         "role": "user",
         "content": "When did humans first land on the Moon?"
       }
     ],
     "temperature": 0.0
   }' | jq
   ```

   The model response

   ```json
   {
     "id": "chatcmpl-Dbdo9WiKJVb2hUh9x9R8mhENLoJ97",
     "object": "chat.completion",
     "created": 3386752496,
     "model": "gpt-4o-mini-2024-07-18",
     "choices": [
       {
         "index": 0,
         "message": {
           "role": "assistant",
           "content": "Humans first landed on the Moon on July 20, 1969, during NASA's Apollo 11 mission. Astronauts Neil Armstrong and Buzz Aldrin became the first and second humans to walk on the lunar surface, while Michael Collins remained in lunar orbit aboard the command module. Armstrong's famous words as he stepped onto the Moon were, \"That's one small step for [a] man, one giant leap for mankind.\"",
           "refusal": null,
           "annotations": []
         },
         "logprobs": null,
         "finish_reason": "stop"
       }
     ],
     "usage": {
       "prompt_tokens": 16,
       "completion_tokens": 87,
       "total_tokens": 103,
       "prompt_tokens_details": {
         "cached_tokens": 0,
         "audio_tokens": 0
       },
       "completion_tokens_details": {
         "reasoning_tokens": 0,
         "audio_tokens": 0,
         "accepted_prediction_tokens": 0,
         "rejected_prediction_tokens": 0
       }
     },
     "service_tier": "default",
     "system_fingerprint": "fp_ff247d5857"
   }
   ```

2. Build the application

   This step assumes that a valid OpenAI API key is available at
   `~/.chatgpt/api.key`. See Step 1 for more information about how to do this.

   ```shell
   ./mvnw clean verify
   ```

   (*Optional*) Verify that the application was created and the dependencies
   copied.

   ```shell
   tree --sort=name './target/libs'
   ```

   The application JAR file and its dependencies.

   ```
   ./target/libs
   ├── annotations-13.0.jar
   ├── classmate-1.7.0.jar
   ├── error_prone_annotations-2.33.0.jar
   ├── jackson-annotations-2.18.2.jar
   ├── jackson-core-2.18.2.jar
   ├── jackson-databind-2.18.2.jar
   ├── jackson-datatype-jdk8-2.18.2.jar
   ├── jackson-datatype-jsr310-2.18.2.jar
   ├── jackson-module-kotlin-2.18.2.jar
   ├── jsonschema-generator-4.38.0.jar
   ├── jsonschema-module-jackson-4.38.0.jar
   ├── jsonschema-module-swagger-2-4.38.0.jar
   ├── kotlin-reflect-1.8.10.jar
   ├── kotlin-stdlib-1.8.0.jar
   ├── kotlin-stdlib-common-1.8.0.jar
   ├── kotlin-stdlib-jdk7-1.8.0.jar
   ├── kotlin-stdlib-jdk8-1.8.0.jar
   ├── logging-interceptor-4.12.0.jar
   ├── okhttp-4.12.0.jar
   ├── okio-3.6.0.jar
   ├── okio-jvm-3.6.0.jar
   ├── openai-java-4.33.0.jar
   ├── openai-java-client-okhttp-4.33.0.jar
   ├── openai-java-core-4.33.0.jar
   ├── prompt-chatgpt-api-1.0.0.jar
   ├── slf4j-api-2.0.16.jar
   └── swagger-annotations-2.2.31.jar

   1 directory, 27 files
   ```

3. Run the program

   This step assumes that a valid OpenAI API key is available at
   `~/.chatgpt/api.key`. See Step 1 for more information about how to do this.

   Run the example with the default prompt.

   ```shell
   java \
     --class-path './target/libs/*' \
     demo.Main
   ```

   Although the prompt remains the same, the output may vary with each run.

   ```
   prompt> When did humans first land on the Moon?
   assistant> Humans first landed on the Moon on July 20, 1969, during the Apollo 11 mission. Astronauts Neil Armstrong and Edwin "Buzz" Aldrin became the first and second humans to walk on the lunar surface.
   ```

   Run the example again with specific prompt.

   ```shell
   java \
     --class-path './target/libs/*' \
     demo.Main 'Who was the first person to walk on the Moon?'
   ```

   The model should respond with 'Neil Armstrong', as he was the first person to
   walk on the Moon.

   ```
   prompt> Who was the first person to walk on the Moon?
   assistant> The first person to walk on the Moon was Neil Armstrong. He did so on July 20, 1969, during the Apollo 11 mission.
   ```
